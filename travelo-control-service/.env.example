# Modules enabled at deploy time. Comma-separated list of module keys (see modules_configs.json).
# always_on=true modules (BACKOFFICE, FINANCE, MANAGEMENT) are always enabled regardless.
# Children with `requires` are auto-enabled when parent is listed (e.g. listing BOAT pulls BOAT_SALES/DISPATCHER/KAPETAN).
TRAVELO_MODULES=BOAT

# Deploy 2026-09-07 (b563928 → 79607b2, 43 commita)

Sto je u ovom izdanju: predlosci PDF karte po prodajnom kanalu, kontrola kopija
karata (novi modul KONTROLA), jedinstven broj karte s oznakom original/kopija,
logo na ispisu po naplatnom uredaju, izmjene na desk aplikaciji i mobilna 1.0.8.

## Koraci

```bash
ssh deploy@46.101.176.117
cd ~/TraveloApp            # ili gdje stoji radna kopija

git pull

# Kljuc za slanje mailova vise ne stoji u kodu (Task #369). Bez njega servis
# radi, ali mail ne odlazi.
grep -q RESEND_API_KEY ~/.profile || echo "export RESEND_API_KEY='...'" >> ~/.profile
source ~/.profile

# Migracije su idempotentne (IF NOT EXISTS). Test VM i lokalni dev dijele istu
# bazu, pa su vec odradene — ovo je za svaki slucaj i za druge instalacije.
node travelo-transactions-service/migrate_tickets_copy_suffix.js
node travelo-transactions-service/migrate_ticket_copy_prints.js
node travelo-transactions-service/migrate_ticket_validations.js
node travelo-transactions-service/migrate_ticket_control_types.js
node travelo-transactions-service/migrate_ticket_code_unique.js
node travelo-boat-service/migrate_ticket_templates.js

# Portal: ako VM vrti SPA_MODE=preview, treba novi build
( cd travelo-portal && npm run build )

# Control-service prvi (nosi katalog modula s novom karticom KONTROLA), pa ostali
pm2 restart all --update-env
```

## Mobilna 1.0.8

APK i `downloads.json` nisu u gitu (mapa je gitignorana), pa se prenose rucno:

```bash
scp travelo-mobile/android/app/build/outputs/apk/release/travelo_app-v1.0.8.apk \
    deploy@46.101.176.117:~/TraveloApp/travelo-web_portal-service/downloads/TraveloAPP-Boat-Mobile-1.0.8.apk
# na VM-u: u downloads.json podici verziju na 1.0.8 i maknuti stari zapis,
# stari APK premjestiti u downloads_archive/
```

## Nakon deploya provjeriti

- Brod → Predlosci karata: popis kanala i predlozaka, ogledni PDF se otvara
- KONTROLA → Kopije karata: modul vidljiv onima kojima je dodijeljen
- Ponovni ispis karata iz POS-a: broj karte na PDF-u ima oznaku kopije
- Mail s racunom i kartama odlazi (provjerava RESEND_API_KEY)
- Nocni prolaz u 03:00 (partnerske fakture i izvjestaj o proviziji)

## Zaostalo

Kljuc koji je do sada stajao upisan u kodu treba zamijeniti novim u Resend
panelu: bio je u javnoj povijesti repozitorija.

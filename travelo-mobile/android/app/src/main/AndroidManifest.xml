<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- AKD Tessera čitanje otočnih iskaznica preko ugrađenog NFC -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!-- Sunmi internal printer service + QR scanner activity visibility (Android 11+) -->
    <queries>
        <package android:name="woyou.aidlservice.jiuiv5" />
        <package android:name="com.sunmi.sunmiqrcodescanner" />
        <intent>
            <action android:name="woyou.aidlservice.jiuiv5.IWoyouService" />
        </intent>
        <intent>
            <action android:name="com.summi.scan" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true"
      tools:replace="android:label,android:theme,android:icon,android:roundIcon">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:screenOrientation="portrait"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- AKD otočne iskaznice — MIFARE DESFire čip detektira se preko TECH_DISCOVERED -->
        <intent-filter>
            <action android:name="android.nfc.action.TECH_DISCOVERED" />
        </intent-filter>
        <meta-data
            android:name="android.nfc.action.TECH_DISCOVERED"
            android:resource="@xml/akd_nfc_tech_filter" />
      </activity>

      <!-- Tessera AAR isporučuje vlastitu hr.akd.tesserademo.MainActivity s LAUNCHER
           intent-filterom — bez tools:node="replace" Android bi je pokrenuo umjesto
           naše MainActivity. Roko demo radi isto. -->
      <activity
          android:name="hr.akd.tesserademo.MainActivity"
          android:exported="false"
          tools:node="replace">
      </activity>
    </application>
</manifest>
